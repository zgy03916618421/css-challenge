{
  "name": "css-chanllenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run sass:watch\" \"npm run live-server\"",
    "sass:watch": "sass --watch scss:css",
    "live-server": "live-server --port=3000 --ignore=**/scss/**,**/.git/**,**/node_modules/**"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.0",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "sass": "^1.97.2"
  }
}
